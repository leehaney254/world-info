import React from 'react';
import { BsSearch } from 'react-icons/bs';

const Form = () => (
  <form>
    <input type="text" placeholder="Eg Kenya, England" />
    <BsSearch />
  </form>
);

export default Form;
